#include <vector>
#include "occupalauney.h"

using std::vector;

// Template declaration and definition can not be separated

// template <typename T>
// void pushUnique(vector<T> &vec, T elt) {
//     bool isin = false;

//     for (T thing : vec) {
//         if (thing == elt) {
//             isin = true;
//             break;
//         }
//     }

//     if (!isin) vec.push_back(elt);
// }